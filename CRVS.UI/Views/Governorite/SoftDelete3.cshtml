@model IEnumerable<CRVS.Core.Models.Governorite>
@{
    ViewData["Title"] = "SoftDelete3";
}

<h1>SoftDelete3</h1>

<p>
    <a asp-action="Create">Create New</a>

</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GovernoriteName)
            </th>


            <th>
                @Html.DisplayNameFor(model => model.IsDeleted3)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.GovernoriteName)
                </td>


                <th>

                    @if (item.IsDeleted3 == true)
                    {
                        <a asp-action="Delete" asp-route-id="@item.GovernoriteId" class="text-success">Delete</a>
                    }

                    else
                    {
                        <a asp-action="Recovary" asp-route-id="@item.GovernoriteId" class="text-danger">Not Delete</a>


                    }


                </th>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.GovernoriteId">Edit</a>
                    <a asp-action="Details" asp-route-id="@item.GovernoriteId">Details</a>
                    @*<a asp-action="Delete" asp-route-id="@item.DohId">Delete</a>*@
                    <a asp-action="History" asp-route-id="@item.GovernoriteId">History</a>
                    <a asp-action="SoftDelete" asp-route-id="@item.GovernoriteId">SoftDelete</a>
                    <a asp-action="SoftDelete3" asp-route-id="@item.GovernoriteId">SoftDelete3</a>
                </td>
            </tr>
        }
    </tbody>
</table>


