@model IEnumerable<CRVS.Core.Models.District>
@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
    <a asp-action="AllActivate">Activate</a>
    <a asp-action="AllNotActivate">NotActivate</a>
    <a asp-action="Recovary">Deleted</a>
    <a asp-action="Recovary2">Deleted2</a>
    <a asp-action="Recovary3">Deleted3</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DistrictName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsActive)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.DistrictName)
                </td>
              

                <th>

                    @if (item.IsActive == true)
                    {
                        <a asp-action="DeActivate" asp-route-id="@item.DistrictId" class="text-success">Active</a>
                    }

                    else
                    {
                        <a asp-action="Activate" asp-route-id="@item.DistrictId" class="text-danger">Not Active</a>


                    }


                </th>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.DistrictId">Edit</a>
                    <a asp-action="Details" asp-route-id="@item.DistrictId">Details</a>
                    <a asp-action="Delete" asp-route-id="@item.DistrictId">Delete</a>
                    <a asp-action="History" asp-route-id="@item.DistrictId">History</a>
                    <a asp-action="SoftDelete" class="btn btn-danger" asp-route-id="@item.DistrictId">Soft Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>


