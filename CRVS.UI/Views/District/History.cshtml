@model IEnumerable<CRVS.Core.Models.DistrictHistory>
@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DistrictHistoryName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.DistrictHistoryName)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.DistrictHistoryId">Edit</a>
                    <a asp-action="Details" asp-route-id="@item.DistrictHistoryId">Details</a>
                    <a asp-action="Delete" asp-route-id="@item.DistrictHistoryId">Delete</a>
                    <a asp-action="History" asp-route-id="@item.DistrictHistoryId">History</a>
                </td>
            </tr>
        }
    </tbody>
</table>


