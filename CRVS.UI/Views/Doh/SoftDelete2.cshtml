@model IEnumerable<CRVS.Core.Models.Doh>
@{
    ViewData["Title"] = "SoftDelete2";
}

<h1>SoftDelete2</h1>

<p>
    <a asp-action="Create">Create New</a>

</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DohName)
            </th>


            <th>
                @Html.DisplayNameFor(model => model.IsDeleted2)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.DohName)
                </td>


                <th>

                    @if (item.IsDeleted2 == true)
                    {
                        <a asp-action="Delete" asp-route-id="@item.DohId" class="text-success">Delete</a>
                    }

                    else
                    {
                        <a asp-action="Recovary" asp-route-id="@item.DohId" class="text-danger">Not Delete</a>


                    }


                </th>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.DohId">Edit</a>
                    <a asp-action="Details" asp-route-id="@item.DohId">Details</a>
                    <a asp-action="Delete" asp-route-id="@item.DohId">Delete</a>
                    <a asp-action="History" asp-route-id="@item.DohId">History</a>
                    <a asp-action="SoftDelete2" asp-route-id="@item.DohId">SoftDelete2</a>
                </td>
            </tr>
        }
    </tbody>
</table>


